@extends('layouts.app',['title'=>'Cajón','heading'=>'Aperturas de cajón'])
@section('content')<div class="flex justify-between gap-4"><div><p class="eyebrow">Hardware</p><h1 class="mt-1 text-3xl font-semibold">Aperturas de cajón</h1><p class="mt-2 text-stone-500">Toda apertura manual queda registrada con empleado y hora. Un error de hardware nunca afecta al pago.</p></div><a class="button-secondary" href="{{ route('restaurant.hardware',$restaurant) }}">Hardware</a></div><div class="mt-8 space-y-2">@forelse($events as $event)<article class="settings-card p-4 text-sm"><p><strong>{{ $event->trigger === 'sale' ? 'Automática por cobro en efectivo' : 'Manual' }}</strong> · {{ $event->employee?->display_name ?? '—' }} · {{ $event->created_at->format('d/m/Y H:i') }} · <span class="font-semibold">{{ $event->status }}</span></p>@if($event->error)<p class="text-red-700">{{ $event->error }}</p>@endif</article>@empty<div class="empty-panel">Sin aperturas registradas.</div>@endforelse</div><div class="mt-4">{{ $events->links() }}</div>@endsection
