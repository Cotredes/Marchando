<div class="mt-8 flex gap-2 overflow-x-auto border-b border-stone-200" aria-label="Sección de restaurante"><a href="{{ route('restaurant.restaurant', $restaurant) }}" class="local-tab {{ request()->routeIs('restaurant.restaurant') ? 'local-tab-active' : '' }}" @if (request()->routeIs('restaurant.restaurant')) aria-current="page" @endif>Mesas</a>@if ($canManage)<a href="{{ route('restaurant.restaurant.manage', $restaurant) }}" class="local-tab {{ request()->routeIs('restaurant.restaurant.manage') ? 'local-tab-active' : '' }}" @if (request()->routeIs('restaurant.restaurant.manage')) aria-current="page" @endif>Administrar</a>@endif</div>
