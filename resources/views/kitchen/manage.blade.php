@extends('layouts.app',['title'=>'Estaciones','heading'=>'Cocina'])
@section('content')<div class="flex justify-between gap-4"><div><p class="eyebrow">Cocina</p><h1 class="mt-1 text-3xl font-semibold">Estaciones</h1></div><a class="button-secondary" href="{{ route('restaurant.kitchen',$restaurant) }}">Ver KDS</a></div>@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif@if($errors->any())<div class="mt-5 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif<div class="mt-8 grid gap-6 lg:grid-cols-[1fr_320px]"><section class="space-y-3">@forelse($stations as $station)<article class="settings-card p-5"><div class="flex justify-between"><div><h2 class="font-semibold">{{ $station->name }}</h2><p class="text-sm text-stone-500">{{ $station->is_active ? 'Activa' : 'Inactiva' }} · {{ $station->items()->count() }} trabajos históricos</p></div>@if(!$station->trashed())<form method="POST" action="{{ route('restaurant.kitchen.stations.destroy',[$restaurant,$station]) }}">@csrf @method('DELETE')<button class="text-button">Archivar</button></form>@endif</div></article>@empty<div class="empty-panel">No hay estaciones.</div>@endforelse</section><section class="settings-card"><form method="POST" action="{{ route('restaurant.kitchen.stations.store',$restaurant) }}">@csrf<div class="settings-card-body space-y-4"><div><label class="field-label">Nueva estación</label><input class="form-input" name="name" placeholder="Ej. Barra" required></div><input type="hidden" name="is_active" value="1"><input type="hidden" name="warning_after_seconds" value="600"><input type="hidden" name="late_after_seconds" value="1200"><button class="button-primary w-full">Crear estación</button></div></form></section></div>@endsection
