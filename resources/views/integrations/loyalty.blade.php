@extends('layouts.app',['title'=>'Fidelización','heading'=>'Fidelización'])
@section('content')<div class="flex justify-between gap-4"><div><p class="eyebrow">Promociones</p><h1 class="mt-1 text-3xl font-semibold">Programa de sellos</h1><p class="mt-2 text-stone-500">Compra X → recompensa. Solo clientes identificados. Un Split Bill con varios pagos genera un único progreso. Participar no implica consentimiento de marketing.</p></div><a class="button-secondary" href="{{ route('restaurant.integrations',$restaurant) }}">Centro</a></div>@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-5 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif<div class="mt-8 grid gap-6 lg:grid-cols-[1fr_320px]"><section class="space-y-3">@forelse($programs as $program)<article class="settings-card p-5"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-semibold">{{ $program->name }}</h2><p class="text-sm text-stone-500">Objetivo {{ $program->target_type === 'product' ? 'producto' : 'categoría' }} #{{ $program->target_id }} · meta {{ $program->goal }} sellos · recompensa: {{ $program->rewardProduct?->name ?? '—' }} · {{ $program->is_active ? 'activo' : 'inactivo' }}</p></div><form method="POST" action="{{ route('restaurant.loyalty.update',[$restaurant,$program]) }}">@csrf @method('PATCH')<button class="button-secondary" name="is_active" value="{{ $program->is_active ? 0 : 1 }}">{{ $program->is_active ? 'Desactivar' : 'Activar' }}</button></form></div></article>@empty<div class="empty-panel">Sin programas. Crea «10.º café gratis».</div>@endforelse</section><section class="settings-card"><form method="POST" action="{{ route('restaurant.loyalty.store',$restaurant) }}">@csrf<div class="settings-card-body space-y-4"><h2 class="font-semibold">Nuevo programa</h2><input class="form-input" name="name" placeholder="Ej. 10.º café gratis" required><div class="grid grid-cols-2 gap-3"><select class="form-input" name="target_type"><option value="product">Producto</option><option value="category">Categoría</option></select><input class="form-input" type="number" name="target_id" min="1" placeholder="ID objetivo" required></div><div class="grid grid-cols-2 gap-3"><input class="form-input" type="number" name="goal" min="2" max="100" value="10" required><select class="form-input" name="reward_product_id">@foreach($products as $product)<option value="{{ $product->id }}">{{ $product->name }}</option>@endforeach</select></div><button class="button-primary w-full">Crear programa</button></div></form></section></div>@endsection
