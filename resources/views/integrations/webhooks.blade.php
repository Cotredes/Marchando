@extends('layouts.app',['title'=>'Webhooks','heading'=>'Webhooks salientes'])
@section('content')<div class="flex justify-between gap-4"><div><p class="eyebrow">Integraciones</p><h1 class="mt-1 text-3xl font-semibold">Webhooks de salida</h1><p class="mt-2 text-stone-500">Eventos firmados (HMAC-SHA256, cabecera X-Marchando-Signature). Un endpoint caído nunca bloquea el TPV.</p></div><a class="button-secondary" href="{{ route('restaurant.integrations',$restaurant) }}">Centro</a></div>@if(session('status'))<div class="mt-5 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('status') }}</div>@endif @if($errors->any())<div class="mt-5 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif<div class="mt-8 grid gap-6 lg:grid-cols-[1fr_320px]"><section class="space-y-3">@forelse($endpoints as $endpoint)<article class="settings-card p-5"><div class="flex flex-wrap justify-between gap-3"><div><h2 class="font-semibold break-all">{{ $endpoint->url }}</h2><p class="text-sm text-stone-500">{{ $endpoint->is_active ? 'activo' : 'inactivo' }} · {{ $endpoint->deliveries_count }} entregas · eventos: {{ implode(', ',$endpoint->events ?? ['todos']) }}</p></div><div class="flex gap-2"><form method="POST" action="{{ route('restaurant.integrations.webhooks.retry',[$restaurant,$endpoint]) }}">@csrf<button class="button-secondary">Reintentar pendientes</button></form><form method="POST" action="{{ route('restaurant.integrations.webhooks.destroy',[$restaurant,$endpoint]) }}" onsubmit="return confirm('¿Eliminar endpoint?')">@csrf @method('DELETE')<button class="text-button">Eliminar</button></form></div></div></article>@empty<div class="empty-panel">Sin endpoints.</div>@endforelse</section><section class="settings-card"><form method="POST" action="{{ route('restaurant.integrations.webhooks.store',$restaurant) }}">@csrf<div class="settings-card-body space-y-4"><h2 class="font-semibold">Nuevo endpoint</h2><input class="form-input" name="url" type="url" placeholder="https://..." required><fieldset><legend class="text-sm font-semibold">Eventos (vacío = todos)</legend>@foreach($events as $event)<label class="check-label"><input type="checkbox" name="events[]" value="{{ $event }}"> {{ $event }}</label>@endforeach</fieldset><button class="button-primary w-full">Registrar</button></div></form></section></div>@endsection
