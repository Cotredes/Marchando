@extends('layouts.app', ['title' => 'Corregir jornada', 'heading' => 'Corregir jornada'])
@section('content')
<div class="max-w-2xl"><p class="eyebrow">Corrección auditada</p><h2 class="mt-1 text-3xl font-semibold tracking-tight">Corregir jornada</h2><p class="mt-3 text-stone-500">La modificación conservará los valores anteriores, el motivo y tu identidad.</p></div>
<form method="POST" action="{{ route('restaurant.staff.attendance.correct', [$restaurant, $interval]) }}" class="mt-8 max-w-2xl rounded-2xl border border-stone-200 bg-white"><div class="settings-card-body space-y-5">@csrf @method('PATCH')<div><label class="field-label" for="started_at">Entrada</label><input class="form-input" type="datetime-local" id="started_at" name="started_at" required value="{{ old('started_at', $interval->started_at->setTimezone($restaurant->timezone)->format('Y-m-d\TH:i')) }}"></div><div><label class="field-label" for="ended_at">Salida</label><input class="form-input" type="datetime-local" id="ended_at" name="ended_at" value="{{ old('ended_at', $interval->ended_at?->setTimezone($restaurant->timezone)->format('Y-m-d\TH:i')) }}"><p class="field-help">Déjalo vacío para mantener una jornada abierta.</p></div><div><label class="field-label" for="reason">Motivo</label><textarea class="form-input" id="reason" name="reason" required rows="3">{{ old('reason') }}</textarea></div>@if($errors->any())<p class="field-message">{{ $errors->first() }}</p>@endif</div><div class="settings-card-footer flex justify-between"><a class="button-secondary" href="{{ route('restaurant.staff.employees.show', [$restaurant, $interval->employee]) }}">Cancelar</a><button class="button-primary">Guardar corrección</button></div></form>
@endsection
