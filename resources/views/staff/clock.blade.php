@extends('layouts.app', ['title' => 'Terminal de fichaje', 'heading' => 'Terminal de fichaje'])
@section('content')
<div class="mx-auto max-w-4xl"><div class="text-center"><p class="eyebrow">{{ $restaurant->name }}</p><h2 class="mt-2 text-4xl font-semibold tracking-tight">Control horario</h2><p class="mt-3 text-stone-500">Selecciona tu nombre para fichar en segundos.</p></div>@if(session('clock_status'))<div class="mx-auto mt-6 max-w-md rounded-2xl border border-emerald-200 bg-emerald-50 px-5 py-4 text-center text-lg font-semibold text-emerald-800" role="status">✓ {{ session('clock_status') }}</div>@endif @if($errors->any())<div class="mx-auto mt-6 max-w-md rounded-2xl border border-red-200 bg-red-50 px-5 py-4 text-center text-red-700" role="alert">{{ $errors->first() }}</div>@endif
@if(!$selected)<div class="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($employees as $employee)<form method="POST" action="{{ route('restaurant.staff.clock.identify', $restaurant) }}" class="rounded-2xl border border-stone-200 bg-white p-5 shadow-sm">@csrf<input type="hidden" name="employee_id" value="{{ $employee->id }}"><label class="block text-center"><span class="block text-2xl font-semibold">{{ $employee->display_name }}</span><span class="mt-1 block text-sm text-stone-500">{{ $employee->roleNames() }}</span><input class="form-input mt-5 text-center text-2xl tracking-[0.4em]" name="pin" type="password" inputmode="numeric" pattern="[0-9]{4,8}" maxlength="8" placeholder="PIN" required aria-label="PIN de {{ $employee->display_name }}"></label><button class="button-primary mt-4 w-full py-3 text-lg">Continuar</button></form>@empty<div class="empty-panel sm:col-span-2 lg:col-span-3">No hay empleados activos con PIN configurado.</div>@endforelse</div>@else<div class="mx-auto mt-10 max-w-md rounded-3xl border border-stone-200 bg-white p-8 text-center shadow-sm"><p class="text-3xl font-semibold">{{ $selected->display_name }}</p>@if($selected->openInterval()->exists())<p class="mt-4 text-lg text-emerald-700">Trabajando desde {{ $selected->openInterval()->first()->started_at->setTimezone($restaurant->timezone)->format('H:i') }}</p><p class="mt-1 text-sm text-stone-500">Pulsa para finalizar tu jornada.</p>@else<p class="mt-4 text-lg text-stone-600">Fuera de jornada</p><p class="mt-1 text-sm text-stone-500">Pulsa para iniciar tu jornada.</p>@endif<form method="POST" action="{{ route('restaurant.staff.clock.punch', $restaurant) }}">@csrf<button class="button-primary mt-8 w-full py-5 text-xl">{{ $selected->openInterval()->exists() ? 'Finalizar jornada' : 'Iniciar jornada' }}</button></form><form method="POST" action="{{ route('restaurant.staff.clock.forget', $restaurant) }}">@csrf<button class="mt-4 text-button">Cambiar empleado</button></form></div>@endif</div>
@endsection
