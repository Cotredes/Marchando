@extends('layouts.pos', ['title' => 'Abrir '.$diningTable->name])
@section('content')
<div class="mx-auto max-w-3xl"><a class="text-button" href="{{ route('restaurant.pos', $restaurant) }}">← Volver a mesas</a><div class="mt-5 rounded-3xl border border-stone-200 bg-white p-6 shadow-sm sm:p-8"><p class="eyebrow">{{ $diningTable->zone->name }}</p><h1 class="mt-1 text-3xl font-semibold">Abrir {{ $diningTable->name }}</h1><p class="mt-2 text-stone-500">Identifica al camarero que inicia la cuenta.</p>@if($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700" role="alert">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('restaurant.pos.tables.store', [$restaurant, $diningTable]) }}" class="mt-8 space-y-6">@csrf<div><label class="field-label" for="guest_count">Comensales <span class="font-normal text-stone-400">(opcional)</span></label><input class="form-input max-w-xs" id="guest_count" name="guest_count" type="number" min="1" max="999" inputmode="numeric" value="{{ old('guest_count') }}"></div><fieldset><legend class="field-label">Camarero</legend><div class="grid gap-3 sm:grid-cols-3">@foreach($employees as $employee)<label class="pos-employee-card"><input class="sr-only peer" type="radio" name="employee_id" value="{{ $employee->id }}" @checked(old('employee_id') == $employee->id) required><span class="block text-xl font-semibold">{{ $employee->display_name }}</span><span class="mt-1 block text-sm text-stone-500">{{ $employee->roleNames() }}</span></label>@endforeach</div></fieldset><div class="max-w-xs"><label class="field-label" for="pin">PIN operativo</label><input class="form-input text-center text-2xl tracking-[.35em]" id="pin" name="pin" type="password" inputmode="numeric" pattern="[0-9]{4,8}" maxlength="8" required></div><button class="button-primary w-full py-4 text-lg sm:w-auto">Abrir cuenta</button></form></div></div>
@endsection
