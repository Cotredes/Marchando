@extends('layouts.pos', ['title' => 'TPV'])
@section('content')
<div class="flex flex-wrap items-end justify-between gap-4"><div><p class="eyebrow">{{ $restaurant->name }}</p><h1 class="mt-1 text-3xl font-semibold tracking-tight">Mesas</h1><p class="mt-2 text-stone-500">Selecciona una mesa para abrir o continuar su cuenta.</p></div><a class="button-secondary" href="{{ route('restaurant.restaurant', $restaurant) }}">Ver plano físico</a></div>
@if(session('status'))<div class="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800" role="status">{{ session('status') }}</div>@endif
@if($errors->any())<div class="mt-5 rounded-xl border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700" role="alert">{{ $errors->first() }}</div>@endif
<div class="mt-8 flex gap-2 overflow-x-auto pb-2"><a href="{{ route('restaurant.pos', $restaurant) }}" class="pos-category-button {{ !request('zone') ? 'pos-category-button-active' : '' }}">Todas</a>@foreach($zones as $zone)<a href="{{ route('restaurant.pos', [$restaurant, 'zone' => $zone->id]) }}" class="pos-category-button {{ request('zone') == $zone->id ? 'pos-category-button-active' : '' }}">{{ $zone->name }}</a>@endforeach</div>
<div class="mt-5 grid gap-6">@foreach($zones as $zone) @if(!request('zone') || request('zone') == $zone->id)<section><div class="mb-3 flex items-center justify-between"><h2 class="text-xl font-semibold">{{ $zone->name }}</h2><span class="text-sm text-stone-500">{{ ($tables[$zone->id] ?? collect())->count() }} mesas</span></div><div class="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6">@foreach($tables[$zone->id] ?? [] as $table) @if($table->activeAssignment?->order)<a href="{{ route('restaurant.pos.orders.show', [$restaurant, $table->activeAssignment->order]) }}" class="pos-table-card pos-table-occupied"><span class="text-lg font-semibold">{{ $table->name }}</span><span class="mt-2 text-sm font-semibold text-orange-700">Ocupada</span><span class="mt-1 text-sm text-stone-700">{{ \App\CatalogMoney::format($table->activeAssignment->order->total_minor) }} €</span><span class="mt-2 text-xs text-stone-500">{{ $table->activeAssignment->order->currentEmployee?->display_name }}</span></a>@else<a href="{{ route('restaurant.pos.tables.open', [$restaurant, $table]) }}" class="pos-table-card"><span class="text-lg font-semibold">{{ $table->name }}</span><span class="mt-2 text-sm text-emerald-700">Libre</span><span class="mt-3 text-xs text-stone-400">Abrir mesa</span></a>@endif @endforeach</div></section>@endif @endforeach</div>
@endsection
