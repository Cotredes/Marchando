@extends('layouts.pos', ['title' => 'Trasladar cuenta'])
@section('content')<div class="mx-auto max-w-2xl"><a class="text-button" href="{{ route('restaurant.pos.orders.show', [$restaurant, $order]) }}">← Volver</a><div class="mt-5 rounded-3xl border border-stone-200 bg-white p-6 sm:p-8"><p class="eyebrow">{{ $order->table->name }}</p><h1 class="mt-1 text-3xl font-semibold">Trasladar cuenta</h1><p class="mt-2 text-stone-500">La cuenta y sus snapshots se conservarán intactos.</p>@if($errors->any())<div class="mt-5 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{{ $errors->first() }}</div>@endif<form method="POST" action="{{ route('restaurant.pos.orders.transfer.store', [$restaurant, $order]) }}" class="mt-8 space-y-5">@csrf<label class="field-label" for="dining_table_id">Mesa destino</label><select class="form-input" id="dining_table_id" name="dining_table_id" required><option value="">Selecciona una mesa libre</option>@foreach($tables as $table)<option value="{{ $table->id }}">{{ $table->zone->name }} · {{ $table->name }}</option>@endforeach</select><button class="button-primary w-full py-4">Confirmar traslado</button></form></div></div>@endsection
